# Copy this file to .env.deploy and update values.

# Docker registry namespace (Docker Hub username or org)
REGISTRY_USER=your-dockerhub-username

# Image tag to deploy
IMAGE_TAG=latest

# Required by movie-assistant service
HF_API_TOKEN=

# Optional service tuning
INFERENCE_BACKEND=api
LOCAL_MODEL_PATH=
