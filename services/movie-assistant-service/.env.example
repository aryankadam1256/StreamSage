# ============================================================================
# Movie Discovery Assistant - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values
#
# IMPORTANT: Never commit the .env file to git!
# The .gitignore file already excludes it.
# ============================================================================

# HuggingFace API Token (REQUIRED for LLM generation)
# Get your token from: https://huggingface.co/settings/tokens
# Create a "Read" token (free tier gives 1000 requests/hour)
HF_API_TOKEN=your_token_here

# ChromaDB Path (defaults to ../../data/chroma_db)
# CHROMA_DB_PATH=../../data/chroma_db

# Embedding Model (defaults to all-MiniLM-L6-v2)
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# LLM Model from HuggingFace (defaults to Mistral-7B)
# Available free models:
#   - mistralai/Mistral-7B-Instruct-v0.2 (recommended, balanced)
#   - HuggingFaceH4/zephyr-7b-beta (good quality)
#   - microsoft/Phi-3-mini-4k-instruct (faster, smaller)
#   - meta-llama/Meta-Llama-3-8B-Instruct (requires approval on HF)
# LLM_MODEL=mistralai/Mistral-7B-Instruct-v0.2
